<Window
    x:Class="TestApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CountryPicker Test App">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CountryPicker WinUI 3 Test" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button x:Name="ShowPickerButton" Content="Show Country Picker" Click="ShowPickerButton_Click"/>
            <TextBlock x:Name="SelectedCountryText" VerticalAlignment="Center" Text="No country selected"/>
        </StackPanel>

        <Frame x:Name="ContentFrame" Grid.Row="2" Margin="0,20,0,0"/>
    </Grid>
</Window>
